# Music Player App - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        MUSIC PLAYER APP                              │
│                     (ERPNext Custom App)                             │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
            ┌───────▼────────┐         ┌───────▼────────┐
            │   BACKEND      │         │   FRONTEND     │
            │   (Python)     │         │   (HTML/JS)    │
            └───────┬────────┘         └───────┬────────┘
                    │                           │
        ┌───────────┼───────────┐              │
        │           │           │              │
   ┌────▼────┐ ┌───▼────┐ ┌───▼─────┐   ┌────▼─────┐
   │DocTypes │ │  API   │ │ Utils   │   │   Web    │
   │         │ │        │ │         │   │  Pages   │
   └────┬────┘ └───┬────┘ └────┬────┘   └────┬─────┘
        │          │           │              │
        │          │           │              │
```

## 📊 Component Breakdown

### 1. DocTypes (Data Models)

```
┌──────────────────┐
│  YouTube Song    │
├──────────────────┤
│ - title          │
│ - youtube_id     │
│ - thumbnail      │
│ - duration       │
│ - channel        │
│ - description    │
└──────────────────┘
         │
         │ Referenced by
         ▼
┌──────────────────┐
│ Playlist Song    │
│  (Child Table)   │
├──────────────────┤
│ - song           │
│ - song_title     │
│ - duration       │
└──────────────────┘
         │
         │ Part of
         ▼
┌──────────────────┐
│ Music Playlist   │
├──────────────────┤
│ - playlist_name  │
│ - description    │
│ - cover_image    │
│ - songs[]        │
└──────────────────┘
```

### 2. API Integration Flow

```
┌─────────────┐
│   User      │
│  Interface  │
└──────┬──────┘
       │
       │ 1. Search/Add
       ▼
┌─────────────────────┐
│  youtube_api.py     │
├─────────────────────┤
│ search_youtube()    │
│ get_video_details() │
│ add_song_from_url() │
└──────┬──────────────┘
       │
       │ 2. API Call
       ▼
┌─────────────────────┐
│  YouTube Data API   │
│      (v3 - Free)    │
└──────┬──────────────┘
       │
       │ 3. Response
       ▼
┌─────────────────────┐
│  Create/Update      │
│  YouTube Song Doc   │
└─────────────────────┘
```

### 3. Player Architecture

```
┌──────────────────────────────────────────┐
│         Music Player Interface            │
│         (/music-player)                   │
└────────────┬─────────────────────────────┘
             │
    ┌────────┼────────┐
    │        │        │
┌───▼───┐ ┌──▼──┐ ┌──▼───┐
│Search │ │Grid │ │Player│
│ Bar   │ │View │ │Control│
└───┬───┘ └──┬──┘ └──┬───┘
    │        │       │
    │        │       │
    └────────┼───────┘
             │
    ┌────────▼────────┐
    │ YouTube IFrame  │
    │   Player API    │
    └─────────────────┘
```

### 4. File Structure

```
music_player/
│
├── 📄 Documentation
│   ├── README.md              (Main guide)
│   ├── PROJECT_SUMMARY.md     (Overview)
│   ├── YOUTUBE_API_SETUP.md   (API setup)
│   ├── QUICK_REFERENCE.md     (Quick ref)
│   └── setup.sh               (Setup script)
│
├── 📦 music_player/
│   ├── music_player/
│   │   │
│   │   ├── 🗂️ doctype/
│   │   │   ├── youtube_song/
│   │   │   │   ├── youtube_song.json
│   │   │   │   ├── youtube_song.py
│   │   │   │   ├── youtube_song.js
│   │   │   │   └── test_youtube_song.py
│   │   │   │
│   │   │   ├── music_playlist/
│   │   │   │   ├── music_playlist.json
│   │   │   │   ├── music_playlist.py
│   │   │   │   ├── music_playlist.js
│   │   │   │   └── test_music_playlist.py
│   │   │   │
│   │   │   └── playlist_song/
│   │   │       ├── playlist_song.json
│   │   │       └── playlist_song.py
│   │   │
│   │   ├── 🌐 www/
│   │   │   └── music-player/
│   │   │       ├── index.html    (UI)
│   │   │       └── index.py      (Context)
│   │   │
│   │   ├── 🔧 API & Utils
│   │   │   ├── youtube_api.py    (YouTube integration)
│   │   │   ├── demo_data.py      (Sample data)
│   │   │   └── hooks.py          (App hooks)
│   │   │
│   │   └── 📁 Other
│   │       ├── config/
│   │       ├── templates/
│   │       └── public/
│   │
│   └── pyproject.toml
```

### 5. Data Flow

```
┌─────────────┐
│    User     │
└──────┬──────┘
       │
       │ 1. Search "lofi music"
       ▼
┌─────────────────┐
│  Search Input   │
└──────┬──────────┘
       │
       │ 2. JavaScript call
       ▼
┌─────────────────────────┐
│ frappe.call()           │
│ youtube_api.search()    │
└──────┬──────────────────┘
       │
       │ 3. HTTP Request
       ▼
┌─────────────────────────┐
│ YouTube Data API v3     │
└──────┬──────────────────┘
       │
       │ 4. JSON Response
       ▼
┌─────────────────────────┐
│ Display Results         │
│ (Song Cards)            │
└──────┬──────────────────┘
       │
       │ 5. User clicks song
       ▼
┌─────────────────────────┐
│ YouTube IFrame Player   │
│ Loads & Plays Video     │
└─────────────────────────┘
```

### 6. Technology Stack

```
┌─────────────────────────────────────┐
│         PRESENTATION LAYER          │
├─────────────────────────────────────┤
│ HTML5 + CSS3 + Vanilla JavaScript   │
│ YouTube IFrame Player API           │
└─────────────────────────────────────┘
                 │
┌─────────────────────────────────────┐
│         APPLICATION LAYER           │
├─────────────────────────────────────┤
│ Frappe Framework (Python)           │
│ Custom DocTypes & Controllers       │
│ Whitelisted API Methods             │
└─────────────────────────────────────┘
                 │
┌─────────────────────────────────────┐
│         INTEGRATION LAYER           │
├─────────────────────────────────────┤
│ YouTube Data API v3 (REST)          │
│ Requests Library (HTTP Client)      │
└─────────────────────────────────────┘
                 │
┌─────────────────────────────────────┐
│           DATA LAYER                │
├─────────────────────────────────────┤
│ MariaDB (via Frappe ORM)            │
│ DocTypes: Song, Playlist, etc.      │
└─────────────────────────────────────┘
```

### 7. User Journey

```
START
  │
  ├─► Open /music-player
  │     │
  │     ├─► View empty state
  │     │     │
  │     │     └─► Search YouTube
  │     │           │
  │     │           └─► Results displayed
  │     │                 │
  │     │                 └─► Click song → Play
  │     │
  │     └─► Add from URL
  │           │
  │           └─► Song added to library
  │
  ├─► Create Playlist
  │     │
  │     ├─► Go to /app/music-playlist
  │     │     │
  │     │     └─► New Playlist
  │     │           │
  │     │           └─► Add songs
  │     │                 │
  │     │                 └─► Save
  │     │
  │     └─► Play Playlist
  │           │
  │           └─► Songs play sequentially
  │
  └─► Manage Library
        │
        ├─► Browse songs
        │
        ├─► Edit playlists
        │
        └─► Delete items
```

### 8. API Quota Management

```
┌─────────────────────────────────────┐
│     YouTube API Free Tier           │
│     Daily Quota: 10,000 units       │
└─────────────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
┌───────▼──────┐  ┌──────▼────────┐
│   Search     │  │ Video Details │
│ 100 units    │  │   1 unit      │
└──────────────┘  └───────────────┘
        │                 │
        └────────┬────────┘
                 │
        ┌────────▼────────┐
        │  ~100 searches  │
        │      OR         │
        │ ~10,000 details │
        │   per day       │
        └─────────────────┘
```

### 9. Security Model

```
┌─────────────────────────────────────┐
│         User Request                │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│    Frappe Authentication            │
│    (Session-based)                  │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│    @frappe.whitelist()              │
│    (Method-level security)          │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│    DocType Permissions              │
│    (Role-based access)              │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│    YouTube API Key                  │
│    (Server-side only)               │
└─────────────────────────────────────┘
```

### 10. Deployment Architecture

```
┌─────────────────────────────────────┐
│         User Browser                │
└─────────────────────────────────────┘
                 │
                 │ HTTP/HTTPS
                 ▼
┌─────────────────────────────────────┐
│      Bench (Development)            │
│      - Web Server (port 8002)       │
│      - SocketIO (port 9002)         │
│      - Redis Cache                  │
│      - Redis Queue                  │
│      - Background Workers           │
└─────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│         MariaDB Database            │
│      (experiments.bench)            │
└─────────────────────────────────────┘
```

---

## 🎯 Key Features by Layer

### Frontend (User Interface)
- ✨ Modern gradient design
- 🎨 Smooth animations
- 📱 Responsive layout
- 🎵 Real-time player controls
- 🔍 Search interface
- 📚 Playlist navigation

### Backend (Business Logic)
- 🔌 YouTube API integration
- 💾 Data persistence
- 🔐 Authentication & authorization
- 📊 Metadata management
- 🎭 Error handling
- 📝 Logging

### Database (Data Layer)
- 📦 Song metadata storage
- 📚 Playlist management
- 🔗 Relational data (songs ↔ playlists)
- 🔍 Search indexing
- 📈 Change tracking

---

## 🔄 Request/Response Cycle

```
User Action → JavaScript → Frappe Call → Python Method → 
YouTube API → Response → Database → JSON → JavaScript → 
UI Update → User Sees Result
```

**Example: Searching for music**

1. User types "lofi music" and clicks Search
2. JavaScript captures input
3. `frappe.call()` to `youtube_api.search_youtube()`
4. Python method makes HTTP request to YouTube API
5. YouTube returns JSON with video results
6. Python processes and returns data
7. JavaScript receives results
8. DOM updated with song cards
9. User sees search results

---

This architecture provides:
- 🎯 **Separation of concerns**
- 🔄 **Scalability**
- 🔐 **Security**
- 🎨 **Maintainability**
- 📈 **Extensibility**
